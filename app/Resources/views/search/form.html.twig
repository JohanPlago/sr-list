<form action="{{ path("episodes_search") }}" class="o-grid">
    <div class="o-grid__item u-4/5">
        <input type="search"
               name="q"
                {% if searchTerm is defined %} value="{{ searchTerm|e('html_attr') }}"{% endif %}
               class="site-header__search"
               placeholder="Sök efter ett radioprogram...">
    </div>
    <div class="o-grid__item u-1/5">
        <button type="submit" class="svg-icon bare-button">
            <svg class="svg-icon__svg svg-icon__svg--color-primary svg-icon--magnifying-glass--xl">
                <use xlink:href="{{ asset('assets/img/sprites.svg') }}#magnifying-glass"/>
            </svg>
        </button>
    </div>
</form>
